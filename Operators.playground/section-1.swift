// Playground - noun: a place where people can play

import UIKit


//5 + 6 the numbers are opperands and the + is the opperator. If it was a % that would find a remainder rather than represent a percentage.
let unit = "ft"
let height = 12// In feet
let width = 10 // In feet

let area = height * width

//1sq. meter = 1 sq. foot / 10.764
let areaInMeters = Double(area) / 10.764

let chairWidth = 3
let spaceRemaining = width % chairWidth //% calculates the remainder
let chairs = width / chairWidth

let a = 120
let b = 240
let areaInFeet = a/12 * b/12

//basic math above

